CREATE TABLE dbLine (
    id INTEGER PRIMARY KEY NOT NULL,
    label TEXT NOT NULL,
    color INTEGER
);

selectAll:
SELECT * FROM dbLine;

selectSpecific:
SELECT * FROM dbLine WHERE id IN ?;

insert:
REPLACE INTO dbLine VALUES ?;

clear:
DELETE FROM dbLine;
